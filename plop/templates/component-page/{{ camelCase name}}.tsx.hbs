/*=============================================== {{>pascalName }} ===============================================*/

import { {{>pascalName }} } from "@julseb-lib/react"
import { Preview } from "./previews"
import type {  } from "@julseb-lib/react/types"
import type { ILib{{>pascalName }} } from "@julseb-lib/react/component-props"
import type { IPage } from "../../../../types"

export const {{ camelCase name }}: IPage<ILib{{>pascalName }}> = {
    name: "{{>pascalName }}",
    component: {{>pascalName }},
    category: "{{ category }}",
    description: "",
    imports: ["{{>pascalName }}"],
    importTypes: ["ILib{{>pascalName }}"],
    extends: [],
    props: [],
    previews: [
        <Preview key={0} />,
    ],
}
